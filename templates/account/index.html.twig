{% extends 'base.html.twig' %}

{% block title %}Mon compte{% endblock %}

{% block body %}
<div class="container mt-4">
    <div class="row">
        <div class="col-md-12">
            <h1 class="mb-4">Mon compte</h1>
            <p class="lead">Bienvenue {{ app.user.firstname }} {{ app.user.name }} !</p>
            <div class="list-group">
                <a href="{{ path('app_order') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-key"></i> Mes commandes
                </a>
                <a href="{{ path('app_modifyPwd') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-key"></i> Modifier mot de passe
                </a>
                <a href="{{ path('app_logout') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-box-arrow-right"></i> Se déconnecter
                </a>
                <a href="{{ path('app_address') }}" class="list-group-item list-group-item-action">
                    <i class="bi bi-geo-alt"></i> Mes adresses
                </a>
            </div>
        </div>
    </div>
</div>
{% endblock %}
